window.__DOLPHIN_CONFIG__ = {
  // Local development: open the UI at http://localhost:4200 and the backend at http://localhost:8000.
  // Production deployments should replace this with the production API origin or serve behind the same origin.
  apiBase: 'http://localhost:8000',
  wsBase: 'ws://localhost:8000'
};
